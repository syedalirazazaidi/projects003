import React from "react";
interface tpProps {
  rgb: string;
  weight: string;
  index: string;
}
export const SingleColor = ({ rgb, weight, index }: tpProps) => {
  return <div>hi</div>;
};
